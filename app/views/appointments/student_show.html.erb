<% #  This file is app/views/appointments/student_show.html.haml %>
<h1> Appointments </h1>
<table class = "table">
  <thead>
    <tr>
      <th> Staff </th>
      <th> Appointment Date </th>
      <th> Starting Time </th>
      <th> Ending Time </th>
      <th> More Info </th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% for a in @appointments -%>
      <tr>
        <td><%= a.staff.first_name + ' ' + a.staff.last_name -%></td>
        <td><%= a.day -%></td>
        <td><%= ((9*60+40+a.time_slot*20)/60).to_s+':'+((9*60+40+a.time_slot*20)%60).to_s -%></td>
        <td><%= ((9*60+40+(a.time_slot+1)*20)/60).to_s+':'+((9*60+40+(a.time_slot+1)*20)%60).to_s -%></td>
        <td><%= link_to "Details", show_appointment_path(a) %></td>
        <td><%= link_to "Cancel", appointment_student_cancel_path(a) -%></td>
      </tr>
    <% end -%>
  </tbody>
</table>
<% if @appointments.length == 0 -%>
  <center><b>You currently do not have any active appointments.</b></center>
<% end -%>

<div class="pull-right button-group">
  <%= link_to "New Appointment", appointment_student_new_path, class: "button blue" %>
</div>
